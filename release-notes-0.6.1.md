# COMMIX v0.6.1

磁轴键盘力度 + 多格式音频 + 暴力扒谱 + 播放修复

## 演奏
- **磁轴键盘(霍尔效应)力度支持**:WebHID 读取模拟压力 → 按键压力即 MIDI 力度(轻按轻弹/重按重弹),报告偏移自适应(Wooting 报告 ID 4 及国产兼容),已授权设备自动重连,原始报告诊断
- 鼠标中键按下 = 滑音立即归零;琴键只响应左键;点琴键/滚轮自动释放输入框焦点(空格延音不再被焦点拦截)
- 滚轮滑音灵敏度提升(0.006 半音/格)+ 3 秒回中

## 音频格式
- **WAV / MP3 / OGG 全支持**:统一解码入口(魔数自动识别),试听/扒谱/自定义波形全部支持三种格式
- **自定义波形上传**:音频单音 → 自相关周期估计(基频优先防倍频)→ 过零对齐闭合周期 → 归一化波表,弹采样音色

## 扒谱(暴力化)
- 时间分辨率翻倍(hop 512→256,5.8ms/帧)+ 阈值 -60dB 更灵敏 + 最短音符 15ms
- **进度条**:暴力傅里叶逐帧推进,实时显示百分比
- **拍号 1-256 全支持**:1/4 2/4 3/4 … 256/4 任意拍号(.plspmid 格式升 v2,兼容旧文件)
- BPM 直方图聚类 + 慢速优先(八分/十六分流也估出正确拍)

## 转录区
- **打开/播放 .plspmid 移到打开 MIDI 旁**:打开即渲染简谱、自动读取文件内音色(每轨灌入通道引擎)
- **播放按钮智能路由**:打开 .plspmid 后点"播放"直接播文件(用文件音色)
- 清空按钮修复(此前未绑定)
- **力度归一修复**:扒谱/播放/打开三重力度归一——之前保存的文件力度过低(平均 0.08)播放几乎无声,现在最响音符归一 127,正常音量可闻;正常文件不受影响

## 其他
- 波表/示波器/力度曲线/录音波形跟随主题反色(黑白主题纯白线)
- 选项卡按音色/演奏/映射/文件分组,复位移至最右
- 彻底全屏(隐藏菜单栏/任务栏,Esc 退出)
- 窗口 API 权限补齐(全屏功能生效)
